pub const PropsTypeLayer = enum { //enum for distinct type of props aditional layer.
    no_props, //layer consituing the default caracter.
    props, //layer consituing an optional effect.

    eyes, //eyes type layer.

    mouth, //mouth type layer.
    mouth_talking, //mouth when talk layer.

    hat, //hat type layer.

    last_enum_value_not_to_use,
};
